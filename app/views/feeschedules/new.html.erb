<!-- Validation failure messages -->
<% if @feeschedule.errors.any? %>
  <% @feeschedule.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Feeschedule</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_feeschedule" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for description -->
      <div class="form-group">
        <label for="description" class="control-label">
          Description
        </label>

        <input type="text" id="description" name="description" placeholder="Description" class="form-control" value="<%= @feeschedule.description %>">
      </div>

      <button class="btn btn-success">
        Create Feeschedule
      </button>
      or
      <a href="/feeschedules">Cancel</a>
    </form>
  </div>
</div>
